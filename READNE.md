A fullstack CRUD application for managing Kontrahenci (business clients) with dynamic fields, CSV report and full Docker support

Stack:

1. React + Vite
2. .NET 8
3. SQLite
4. Docker

Quick Start:

1. Run command in terminal from project root -> docker-compose up --build
2. Frontend: http://localhost:5173
3. Backend API: http://localhost:5000
